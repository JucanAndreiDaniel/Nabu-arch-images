[Theme]
# maya ships with the sddm package (verified present on-device); the
# breeze SDDM theme is NOT shipped by the Arch breeze package (no
# sddm-theme package in the repos) — Current=breeze falls back to the
# embedded theme and the greeter then dies.
Current=maya
