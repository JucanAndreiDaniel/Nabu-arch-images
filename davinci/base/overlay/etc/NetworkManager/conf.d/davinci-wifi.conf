# davinci WiFi defaults.
# ath10k reports a random MAC every boot ("invalid MAC address; choosing
# random"), so default new WiFi connections to a stable per-machine MAC.
# This keeps DHCP reservations and MAC allow-lists working across reboots.
[connection]
wifi.cloned-mac-address=stable
