# Ini-style configuration file for ukify(1).
# See kernel-install(8) and ukify(1) for details.
# DeviceTree is rewritten by overlay-post-apply / uki-regenerate to the
# installed Samsung DTB (sm7150-xiaomi-davinci-samsung).

[UKI]
DeviceTree=/boot/dtb-linux-davinci
